%!PS-Adobe-2.0 EPSF-2.0
%%BoundingBox: 103 256 492 586

/punt {2 sc div 0 360 arc fill} def

/arrowhead {
   gsave
	currentpoint
	4 2 roll exch 4 -1 roll exch
	sub 3 1 roll sub
	exch atan rotate dup scale
	-7 2 rlineto
	1 -2 rlineto
	-1 -2 rlineto
	closepath fill
   grestore
   newpath
} bind def

298 421 translate

/sc 150 def

sc sc scale

/alpha 0 def
/beta 25 def
/zeta -62 def
/grijs1 1 def
/grijs2 0.55 def

%%%%%% lucht
gsave
.9 setgray
newpath
-1.3 -1.1 moveto
1.3 -1.1 lineto
1.3 1.1 lineto
-1.3 1.1 lineto
closepath
fill
grestore

newpath
0 0 1 0 360 arc
grijs2 setgray
fill
0 setgray


/x0 0 def
/y0 0 def

/xa alpha sin def
/ya alpha cos beta sin mul def
/za alpha cos beta cos mul def

/xb alpha cos def
/yb alpha sin beta sin neg mul def
/zb alpha sin beta cos mul neg def

/xc 0 def
/yc beta cos def
/zc beta sin neg def

.0003 setlinewidth

/a 1 def
/b 1 def
/c 1 def

/x {%
/t exch def
/p exch def
t sin p cos mul a mul } def

/y {%
/t exch def
/p exch def
t sin p sin mul b mul } def

/z {%
/t exch def
/p exch def
t cos c mul } def

/theta 0 def

20 {
 
/phi -92 def

60 {
newpath
phi theta x xa mul phi theta y xb mul add phi theta z xc mul add 
phi theta x ya mul phi theta y yb mul add phi theta z yc mul add moveto
/phi phi 6 add def
phi theta x xa mul phi theta y xb mul add phi theta z xc mul add 
phi theta x ya mul phi theta y yb mul add phi theta z yc mul add lineto
/theta theta 9 add def
phi theta x xa mul phi theta y xb mul add phi theta z xc mul add 
phi theta x ya mul phi theta y yb mul add phi theta z yc mul add lineto
/phi phi 6 sub def
phi theta x xa mul phi theta y xb mul add phi theta z xc mul add 
phi theta x ya mul phi theta y yb mul add phi theta z yc mul add lineto
/theta theta 9 sub def
closepath
/phi phi 3 add def
/theta theta 4 add def
phi cos theta sin mul b mul c mul za mul %
phi sin theta sin mul a mul c mul zb mul add %
theta cos a mul b mul zc mul add 0 gt % 
{ gsave
    phi zeta gt phi zeta 180 add le and 
     {grijs1 setgray} {grijs2 setgray} ifelse
    fill 
  grestore
  stroke
} if
/theta theta 4 sub def
/phi phi 3 add def
} repeat
/theta theta 9 add def
} repeat


1 sc div setlinewidth


%%%%%%%%% pijl vanaf middelpunt naar zonsrichting
.3 setgray
/phi zeta 90 add def
/theta 90 def
newpath
0 0 moveto
phi theta x xa mul phi theta y xb mul add phi theta z xc mul add 
phi theta x ya mul phi theta y yb mul add phi theta z yc mul add lineto
stroke

newpath
phi theta x xa mul phi theta y xb mul add phi theta z xc mul add 
phi theta x ya mul phi theta y yb mul add phi theta z yc mul add moveto
.01 0 0 arrowhead


0 0 punt

showpage

