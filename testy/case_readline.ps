/buff 256 string def
{%loop
    currentfile buff readline not {exit} if
    (%%EOF) anchorsearch {exit} if
    token pop exch token pop exch 3 1 roll moveto
    token pop exch token pop exch pop lineto
    stroke
} bind loop